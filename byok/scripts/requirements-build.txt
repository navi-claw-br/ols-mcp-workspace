# Dependências para build da imagem BYOK
# Instaladas via pip no Containerfile
# (mantido separado para referência / uso local)
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0
numpy>=1.24.0
tiktoken>=0.7.0
langchain-text-splitters>=0.3.0
